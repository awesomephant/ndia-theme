<footer class="footer" tabindex="-1">
    {% if not is_home %}
    <div class="footer__section">
        <p>
            {{site.description | replace({"Neurodiversity in Albertopolis": "<strong>Neurodiversity in Albertopolis</strong>"})}}
        </p>
    </div>
    {% endif %}

    <div class="footer__section">
        {% include "/partial/newsletter-form.twig" with {label: "Join our mailing list for updates on upcoming events and other news.", prefix: "footer"}%}
    </div>

    <nav class="footer__nav">
        {% include "menu.twig" with {'items': menu_primary.get_items} %}
        {% include "menu.twig" with {'items': menu_secondary.get_items} %}
    </nav>
    <div class="copyright">
        <p>
            Header illustration by Roland Ross, site by <a href="https://maxkoehler.com/">MK</a>.
        </p>
        <p>© NDIA {{"now"|date('Y')}}.</p>
    </div>
    <div class="logo__container">
        {% include "partial/cover.svg" %}
    </div>
</footer>